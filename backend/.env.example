# Boardroom Booking Backend - Environment Configuration
# Copy this file to .env and update with your actual values

# ============================================================================
# CORE CONFIGURATION
# ============================================================================

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/boardroom_booking

# Security Configuration
JWT_SECRET=your_very_secure_jwt_secret_key_minimum_32_characters_long

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# ============================================================================
# EMAIL CONFIGURATION (Gmail Example)
# ============================================================================

# Gmail SMTP Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_APP_PASSWORD=your_gmail_app_password_not_regular_password
EMAIL_FROM=noreply@yourdomain.com

# Note: For Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an "App Password" (not your regular Gmail password)
# 3. Use the App Password in EMAIL_APP_PASSWORD

# ============================================================================
# FILE UPLOAD CONFIGURATION (ImageKit)
# ============================================================================

# ImageKit Configuration for file uploads
IMAGEKIT_PUBLIC_KEY=your_imagekit_public_key
IMAGEKIT_PRIVATE_KEY=your_imagekit_private_key
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id

# Get these from: https://imagekit.io/dashboard/developer/api-keys

# ============================================================================
# ERROR TRACKING (Optional)
# ============================================================================

# Sentry Configuration for error tracking and performance monitoring
SENTRY_DSN=https://your_sentry_dsn@sentry.io/project_id

# Get this from: https://sentry.io/settings/projects/your-project/keys/

# ============================================================================
# RATE LIMITING (Optional - defaults applied based on NODE_ENV)
# ============================================================================

# Production values (development uses higher limits):
# RATE_LIMIT_GENERAL_MAX=1000    # General API requests per 15 minutes
# RATE_LIMIT_AUTH_MAX=20         # Authentication requests per 15 minutes  
# RATE_LIMIT_BOOKING_MAX=30      # Booking operations per minute
# RATE_LIMIT_EMAIL_MAX=10        # Email/notification requests per hour

# ============================================================================
# BACKUP CONFIGURATION (Optional)
# ============================================================================

# Backup Settings
BACKUP_DIR=./backups
MAX_BACKUPS=7
BACKUP_COMPRESSION=true

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

# Timezone
TZ=UTC

# ============================================================================
# PRODUCTION ADDITIONAL SETTINGS
# ============================================================================

# Redis Configuration (for production caching)
# REDIS_URL=redis://localhost:6379

# SSL Configuration (for production)
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/key.pem

# ============================================================================
# VALIDATION NOTES
# ============================================================================

# Required for all environments:
# - JWT_SECRET (must be 32+ characters)
# - MONGODB_URI (valid MongoDB connection string)
# - PORT (numeric port number)

# Required for production:
# - All email configuration variables
# - All ImageKit configuration variables
# - SENTRY_DSN (recommended)

# The application will validate these on startup and fail gracefully if missing.